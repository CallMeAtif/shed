track = "A"
pitch = "Reads your package.json, lockfile and source tree offline, and tells you which dependencies the Node standard library already replaced - with the exact API, the version it landed in, and every import site."
language = "javascript"
runtime = "node >=22.17.0"
